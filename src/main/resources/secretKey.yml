# 키변경 필요
jwt:
  expiration:
    day: 10
  token:
    prefix: Bearer
  header:
    string: Authorization
  secret:
    key: secret


# 사용 메일 작성 필요
javax:
  mail:
    host : smtp.gmail.com
    id : empty
    password : empty
    port : 587


#S3 저장소 값 필요
cloud:
  aws:
    s3:
      bucket: empty
    credentials:
      access-key: empty
      secret-key: empty
    region:
      static: ap-northeast-2

#문자 서비스
twilio:
  accountSid: empty
  authToken: empty
  phoneNumber: empty

#구글 API
google:
  geocode:
    api:
      key: empty
      url: empty


